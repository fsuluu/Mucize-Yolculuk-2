import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mucizeyolculuk2.app',
  appName: 'MucizeYolculuk2',
  webDir: 'dist'
};

export default config;
